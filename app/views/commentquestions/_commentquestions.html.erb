<%= link_to t('questions.show.commentquestions'), "##{html_id}",
    class: 'btn btn-primary', data: {'bs-toggle': 'collapse'} %>

<%= tag.div class: 'collapse', id: html_id do%>
    <%= form_with model: [@question, commentquestion],
    class: 'pt-3 border-top my-2' do |f|%>
    <%= render 'shared/errors', object: commentquestion%>

    <div class="mb-3 row">
        <div class="col-sm-2 col-form-label">
            <%=f.label :body %>
        </div>

        <div class="col-sm-10">
            <%= f.text_area :body, class: 'form-control form-control-sm' %>
        </div>
     </div>

    <%= f.submit 'Submit', class: 'btn btn-sm btn-primary'%>

    <% end %>

    <%= render @question.commentquestions %>

<% end %>